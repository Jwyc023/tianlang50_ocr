# ROI坐标系统说明

## 坐标格式
ROI坐标使用格式：`[x, y, width, height]`

## 参数含义

### 📍 x (横坐标)
- **含义**：矩形左上角的水平位置
- **单位**：像素
- **范围**：0 到屏幕宽度
- **说明**：距离屏幕左边缘的距离

### 📍 y (纵坐标)  
- **含义**：矩形左上角的垂直位置
- **单位**：像素
- **范围**：0 到屏幕高度
- **说明**：距离屏幕顶部边缘的距离

### 📏 width (宽度)
- **含义**：矩形的宽度
- **单位**：像素
- **范围**：> 0
- **说明**：从左上角向右延伸的距离

### 📏 height (高度)
- **含义**：矩形的高度
- **单位**：像素
- **范围**：> 0
- **说明**：从左上角向下延伸的距离

## 矩形位置计算

### 四个角的位置：
- **左上角**：(x, y)
- **右上角**：(x + width, y)
- **左下角**：(x, y + height)
- **右下角**：(x + width, y + height)

## 实际示例

假设ROI坐标为：`[100, 200, 150, 80]`

```
屏幕坐标系统：
(0,0) ──────────────────────────────────→ x轴
│
│
│     (100,200) ────────── (250,200)
│           │                    │
│           │     ROI区域        │
│           │                    │
│     (100,280) ────────── (250,280)
│
│
↓
y轴
```

### 计算过程：
- **左上角**：(100, 200)
- **右上角**：(100 + 150, 200) = (250, 200)
- **左下角**：(100, 200 + 80) = (100, 280)
- **右下角**：(100 + 150, 200 + 80) = (250, 280)

## OpenCV中的使用

在OpenCV中，ROI坐标用于：

### 1. 图像裁剪
```python
# 从原图中提取ROI区域
roi_region = img[y:y+height, x:x+width]
```

### 2. 绘制矩形
```python
# 绘制ROI框框
cv2.rectangle(img, (x, y), (x + width, y + height), color, thickness)
```

### 3. 坐标验证
```python
# 检查坐标是否在图像范围内
if x >= 0 and y >= 0 and x + width <= img_width and y + height <= img_height:
    # 坐标有效
    pass
```

## 注意事项

### ⚠️ 坐标系统
- **原点位置**：屏幕左上角 (0, 0)
- **x轴方向**：从左到右递增
- **y轴方向**：从上到下递增

### ⚠️ 边界检查
- x + width 不能超过屏幕宽度
- y + height 不能超过屏幕高度
- 所有坐标值必须 >= 0

### ⚠️ 常见错误
1. **宽度或高度为0**：会导致空区域
2. **负坐标**：会导致越界错误
3. **超出屏幕范围**：会导致截取失败

## 在项目中的应用

### ROI配置文件示例：
```json
{
  "中证1000": {
    "power": [75, 566, 153, 27],    // 主力等级区域
    "grade": [395, 565, 80, 22]     // 级别区域
  }
}
```

### 解释：
- **主力等级区域**：从(75, 566)开始，宽153像素，高27像素
- **级别区域**：从(395, 565)开始，宽80像素，高22像素

这样就能精确定位到需要OCR识别的区域了！
