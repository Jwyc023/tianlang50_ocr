# 时间轴监控功能 - 项目更新说明

## 新增功能

已成功添加**时间轴自动监控功能**，可以自动记录每一分钟8个板块的主力等级和级别。

## 新增文件

### 1. 核心脚本

- **`src/monitor_timeline.py`** - 时间轴监控主程序
  - 自动移动鼠标沿时间轴采集数据
  - 支持测试模式（`--test`）
  - 支持自定义时间点数量（`--test-points`）
  - 支持自定义输出路径（`--output`）

### 2. 配置工具

- **`tools/calibrate_timeline.py`** - 时间轴校准工具
  - 交互式标定时间轴坐标
  - 测试校准结果
  - 生成配置文件

- **`tools/timeline_workflow.py`** - 完整工作流程向导
  - 一键式引导完成所有配置
  - 自动检查依赖和配置
  - 逐步测试和运行

### 3. 配置文件

- **`data/timeline_config.json`** - 时间轴配置（运行校准后生成）
- **`data/timeline_config_example.json`** - 配置文件模板

### 4. 文档

- **`README_时间轴监控.md`** - 快速开始指南
- **`docs/时间轴监控使用说明.md`** - 详细使用手册
- **`时间轴监控功能说明.md`** - 本文件

## 快速开始

### 方法1: 使用工作流程向导（推荐新手）

```bash
cd tools
python timeline_workflow.py
```

向导会自动引导你完成所有步骤。

### 方法2: 手动操作（推荐熟悉用户）

```bash
# 1. 校准时间轴
cd tools
python calibrate_timeline.py

# 2. 测试运行
cd ../src
python monitor_timeline.py --test

# 3. 完整运行
python monitor_timeline.py
```

## 功能特点

### 自动化

- ✅ 自动移动鼠标到时间轴各时间点
- ✅ 自动截图和OCR识别
- ✅ 自动保存数据到CSV
- ✅ 自动计算交易信号

### 智能化

- ✅ 测试模式：只采集部分点验证准确性
- ✅ 命令行参数：灵活配置运行方式
- ✅ 错误处理：识别失败自动记录
- ✅ 安全中断：移动鼠标到左上角即可停止

### 灵活性

- ✅ 可自定义时间范围（默认9:30-15:00）
- ✅ 可自定义采集点数量
- ✅ 可指定输出文件路径
- ✅ 支持多种运行模式

## 使用场景

### 场景1: 历史数据回溯

打开时间轴界面，回放历史行情，使用本工具采集每分钟的数据用于后续分析。

### 场景2: 数据验证

采集一段时间的数据，验证交易策略的历史表现。

### 场景3: 批量采集

可以对多个时间段或多个交易日进行批量数据采集。

## 命令行参数

```bash
# 基本用法
python monitor_timeline.py

# 测试模式（采集5个点）
python monitor_timeline.py --test

# 测试模式（采集10个点）
python monitor_timeline.py --test --test-points 10

# 指定输出文件
python monitor_timeline.py --output ../data/my_data.csv

# 组合使用
python monitor_timeline.py --test --test-points 3 --output ../data/test.csv
```

## 输出示例

### CSV文件格式

```csv
时间,中证1000_主力等级,中证1000_级别,中证500_主力等级,中证500_级别,...,信号
09:30,15.5,AA,12.3,A,...,做多
09:31,16.2,AAA,13.1,AA,...,做多
09:32,14.8,A,11.5,A,...,观望
...
15:00,-8.2,-A,-6.5,-B,...,做空
```

### 文件命名

- 默认：`data/timeline_YYYYMMDD_HHMMSS.csv`
- 自定义：通过`--output`参数指定

## 性能指标

- **采集速度**: 约1-2秒/时间点
- **总时间**: 330个点约10-15分钟
- **准确率**: 取决于OCR配置和界面清晰度
- **资源占用**: CPU和内存占用较低

## 技术实现

### 核心技术

1. **鼠标自动化**: `pyautogui`
2. **屏幕截图**: `mss`
3. **图像处理**: `opencv-python`
4. **OCR识别**: `pytesseract`
5. **数据处理**: `numpy`, `pandas`

### 算法流程

```
1. 加载配置（时间轴坐标、ROI区域）
   ↓
2. 计算时间点和对应X坐标（线性插值）
   ↓
3. 循环处理每个时间点：
   - 移动鼠标到X坐标
   - 等待界面更新（0.3秒）
   - 截图
   - OCR识别8个板块
   - 计算信号
   - 写入CSV
   ↓
4. 完成采集
```

## 注意事项

### 使用前

1. **必须先完成ROI校准**
   - 运行 `tools/calibrate_rois.py`
   - 配置文件：`data/rois.json`

2. **必须先完成时间轴校准**
   - 运行 `tools/calibrate_timeline.py`
   - 配置文件：`data/timeline_config.json`

3. **安装所有依赖**
   ```bash
   pip install pyautogui opencv-python numpy pytesseract mss pillow
   ```

### 使用中

1. **不要操作电脑** - 鼠标会自动移动
2. **保持界面稳定** - 不要调整窗口大小或位置
3. **确保界面可见** - 不要遮挡时间轴和板块区域
4. **建议先测试** - 使用`--test`模式验证准确性

### 故障排查

| 问题 | 原因 | 解决方法 |
|------|------|----------|
| 鼠标位置不准 | 时间轴坐标错误 | 重新校准时间轴 |
| OCR错误率高 | ROI区域不准 | 重新校准ROI |
| 程序报错退出 | 配置文件缺失 | 检查配置文件是否存在 |
| 数据有缺失 | 界面显示延迟 | 增加等待时间 |

## 后续优化方向

- [ ] 支持多显示器配置
- [ ] 添加实时预览功能
- [ ] 支持断点续传
- [ ] 添加数据质量检查
- [ ] 优化OCR识别速度
- [ ] 支持更多时间格式
- [ ] 添加进度条显示
- [ ] 生成数据分析报告

## 版本信息

- **版本**: 1.0.0
- **发布日期**: 2025-10-09
- **作者**: AI Assistant
- **依赖版本**:
  - Python >= 3.7
  - opencv-python >= 4.5.0
  - pytesseract >= 0.3.0
  - pyautogui >= 0.9.0
  - mss >= 6.0.0

## 相关文档

- [快速开始指南](README_时间轴监控.md)
- [详细使用手册](docs/时间轴监控使用说明.md)
- [高精度监控说明](高精度监控使用说明.md)
- [OCR优化方案](OCR_优化方案.md)

---

**提示**: 首次使用建议先阅读快速开始指南，或直接运行工作流程向导。


