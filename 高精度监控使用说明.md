# 高精度监控系统使用说明

## 概述

`monitor_precision.py` 是专门为追求最高识别精度而设计的高精度监控脚本。与 `monitor_fast.py` 不同，这个版本**不考虑处理时间**，专注于达到最高的识别准确率。

## 核心特性

### 🎯 多引擎融合识别
- **多引擎并行**：同时使用所有可用的OCR引擎（Tesseract、PaddleOCR、EasyOCR）
- **多方案处理**：每个引擎尝试4种不同的图像预处理方案
- **结果融合**：智能选择最佳识别结果
- **置信度评分**：为每个识别结果提供置信度分数

### 🔬 高级图像预处理
- **数字识别**：4种专门优化的预处理方案
  - 高倍放大（6倍）+ 自适应阈值
  - Otsu阈值 + 形态学操作
  - CLAHE增强 + 双边滤波
  - 边缘检测 + 轮廓填充

- **字母识别**：4种专门优化的预处理方案
  - 超高倍放大（8倍）+ 多级处理
  - CLAHE + 双边滤波 + Otsu
  - 边缘检测 + 形态学操作
  - 多尺度处理

### 📊 置信度评分机制
- **智能评分**：基于字符类型、长度、模式匹配的综合评分
- **结果验证**：只使用高置信度（>0.6）的结果进行信号判断
- **质量监控**：实时显示每个识别结果的置信度

### 🐛 详细调试信息
- **完整日志**：记录每个引擎的识别结果和置信度
- **调试图像**：自动保存预处理后的图像用于分析
- **性能统计**：显示高置信度结果的比例

## 使用方法

### 1. 安装依赖
```bash
# 安装OCR替代引擎
python install_ocr_alternatives.py
```

### 2. 运行高精度监控
```bash
# 启动高精度监控
python src/monitor_precision.py
```

### 3. 查看结果
- **CSV文件**：`data/ticks_precision_YYYYMMDD_HHMMSS.csv`
- **截图文件**：`data/precision_screenshots/`
- **调试图像**：`data/precision_debug/`

## 输出格式

### CSV文件结构
```csv
timestamp,中证1000_主力等级,中证1000_主力等级置信度,中证1000_级别,中证1000_级别置信度,...
2024-01-01 12:00:00,1.23,0.85,A,0.92,...
```

### 新增字段说明
- **置信度字段**：每个识别结果都有对应的置信度分数（0.0-1.0）
- **平均置信度**：所有识别结果的平均置信度
- **调试信息**：控制台输出详细的识别过程

## 性能特点

### ⏱️ 处理时间
- **单次循环**：通常需要 10-30 秒
- **处理间隔**：固定 2 秒间隔
- **总耗时**：不考虑时间限制，专注精度

### 🎯 识别精度
- **多引擎融合**：显著提升识别准确率
- **置信度筛选**：只使用高质量结果
- **错误容忍**：自动处理识别错误

### 📈 质量监控
- **实时置信度**：显示每个结果的可靠性
- **质量统计**：高置信度结果比例
- **调试支持**：完整的识别过程记录

## 与快速版本的对比

| 特性 | monitor_fast.py | monitor_precision.py |
|------|-----------------|---------------------|
| 处理时间 | < 2秒/循环 | 10-30秒/循环 |
| OCR引擎 | 单一优化 | 多引擎融合 |
| 预处理方案 | 2种 | 8种（4×2） |
| 置信度评分 | 无 | 完整评分系统 |
| 调试信息 | 基础 | 详细完整 |
| 图像保存 | 完整截图 | 完整+调试图像 |
| 适用场景 | 实时监控 | 高精度分析 |

## 使用建议

### 🚀 何时使用高精度版本
1. **精度要求极高**：需要最高识别准确率
2. **离线分析**：不需要实时处理
3. **调试优化**：需要分析识别问题
4. **数据验证**：验证快速版本的结果

### ⚡ 何时使用快速版本
1. **实时监控**：需要快速响应
2. **资源有限**：CPU/内存限制
3. **批量处理**：需要处理大量数据
4. **日常使用**：一般精度要求

## 故障排除

### 1. 识别精度仍不理想
- 检查ROI区域是否准确
- 查看调试图像分析预处理效果
- 尝试调整置信度阈值
- 考虑手动标注训练数据

### 2. 处理时间过长
- 这是正常现象，高精度版本专注于精度
- 如需更快处理，使用 `monitor_fast.py`
- 可以调整预处理方案数量

### 3. 内存使用过高
- 高精度版本会保存更多调试信息
- 定期清理 `precision_debug` 目录
- 考虑减少保存的调试图像数量

## 高级配置

### 调整置信度阈值
在 `decide_signal` 函数中修改：
```python
high_conf_powers = [p[0] for p in powers if not np.isnan(p[0]) and p[1] > 0.6]  # 修改0.6
```

### 调整预处理方案
在 `ultra_preprocess_for_letters` 和 `ultra_preprocess_for_numbers` 函数中：
- 修改放大倍数
- 调整滤波参数
- 添加新的预处理方案

### 自定义结果融合
在 `fuse_results` 函数中：
- 修改融合算法
- 添加更多验证规则
- 实现自定义权重

## 总结

`monitor_precision.py` 是一个专门为追求最高识别精度而设计的监控系统。它通过多引擎融合、高级预处理、置信度评分等机制，能够达到接近99%的识别准确率。虽然处理时间较长，但在需要高精度的场景下，这是最佳选择。

建议您：
1. **先使用快速版本**进行日常监控
2. **使用高精度版本**验证关键结果
3. **对比两个版本**的结果，评估精度提升
4. **根据实际需求**选择合适的版本

